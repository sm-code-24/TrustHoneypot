{
  "name": "trusthoneypot",
  "version": "2.0.0",
  "private": true,
  "description": "TrustHoneypot — Agentic Scam Intelligence Platform (India AI Impact Buildathon PS-2)",
  "scripts": {
    "dev:backend": "uvicorn main:app --reload --host 0.0.0.0 --port 8000 --app-dir app",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently -n API,UI -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "cd frontend && npm install && npm run build",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000 --app-dir app"
  },
  "keywords": [
    "honeypot",
    "scam-detection",
    "fastapi",
    "react",
    "ai-buildathon"
  ],
  "author": "200 Hustlers",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
