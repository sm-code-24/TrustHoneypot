# Railway / Nixpacks configuration
# Force Python provider — frontend is deployed separately on Vercel.
providers = ["python"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} --app-dir app"
